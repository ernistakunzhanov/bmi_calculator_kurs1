import 'package:bmi_calculator/constants/colors/app_colors.dart';
import 'package:flutter/widgets.dart';

class AppDecoration {
  static const Decoration contDecor = BoxDecoration(
    color: AppColors.contColor,
    borderRadius: BorderRadius.all(
      Radius.circular(10),
    ),
  );
}
